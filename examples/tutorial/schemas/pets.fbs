namespace Tutorial;

table Cat {
  name:string (required);
  lives:ubyte = 9;
}

table Dog {
  name:string (required);
  good:bool = true;
}

union Pet { Cat, Dog }

table PetList {
  favorite:Pet;
  residents:[Pet];
}

root_type PetList;
file_identifier "PETS";
